% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_monthly_response_sector.R
\name{compute_monthly_response_sector}
\alias{compute_monthly_response_sector}
\title{Compute Monthly Response by Sector}
\usage{
compute_monthly_response_sector(
  response_data,
  processed_response_data,
  prev_period_date,
  current_period_date,
  start_response_date,
  ind_number,
  response_mapping
)
}
\arguments{
\item{response_data}{A dataframe containing response records, including UUIDs and response start dates.}

\item{processed_response_data}{A dataframe containing processed response data, including sectoral responses.}

\item{prev_period_date}{The start date of the previous period (used for filtering responses).}

\item{current_period_date}{The start date of the current period (used for filtering responses).}

\item{start_response_date}{Column name (as a string) indicating the start date of the response.}

\item{ind_number}{Column name (as a string) indicating the number of individuals assisted.}

\item{response_mapping}{A named list mapping response sector names to corresponding column names in the dataset.}
}
\value{
A dataframe summarizing the monthly RRM responses by sector.
}
\description{
This function aggregates response data by sector for a specified time period, computing
the total number of responses and the total number of individuals assisted for each sector.
}
\examples{
library(dplyr)

# Example response data
response_data <- data.frame(
  uuid = c("A1", "B2", "C3", "D4"),
  start_response_date = as.Date(c("2024-02-01", "2024-02-10", "2024-03-01", "2024-03-05"))
)

# Example processed response data
processed_response_data <- data.frame(
  uuid = c("A1", "B2", "C3", "D4"),
  food_response = c(1, 0, 1, 1),
  wash_response = c(0, 1, 0, 1),
  nfi_response = c(1, 1, 1, 0),
  ind_number = c(100, 200, 150, 250)
)

# Response mapping
response_mapping <- list(
  "Food" = "food_response",
  "WASH" = "wash_response",
  "NFI" = "nfi_response"
)

# Compute monthly sector response summary
summary_df <- compute_monthly_response_sector(
  response_data = response_data,
  processed_response_data = processed_response_data,
  prev_period_date = as.Date("2024-02-01"),
  current_period_date = as.Date("2024-03-01"),
  start_response_date = "start_response_date",
  ind_number = "ind_number",
  response_mapping = response_mapping
)

print(summary_df)
}
