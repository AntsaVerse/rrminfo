% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/join_alerts_responses_data.R
\name{join_alerts_responses_data}
\alias{join_alerts_responses_data}
\title{Join Alerts and Responses Data}
\usage{
join_alerts_responses_data(
  uuid,
  alert_data,
  eval_data,
  rrm_data,
  rrm_response_number,
  postrrm_data,
  postrrm_response_number,
  incident_date,
  validation_date,
  rrm_start_date,
  rrm_end_date,
  postrrm_start_date,
  postrrm_end_date
)
}
\arguments{
\item{uuid}{The unique identifier column name.}

\item{alert_data}{The alert dataset.}

\item{eval_data}{The evaluation dataset.}

\item{rrm_data}{The RRM dataset.}

\item{rrm_response_number}{The column representing the number of RRM responses.}

\item{postrrm_data}{The Post-RRM dataset.}

\item{postrrm_response_number}{The column representing the number of Post-RRM responses.}

\item{incident_date}{The column representing the incident date.}

\item{validation_date}{The column representing the validation date.}

\item{rrm_start_date}{The column representing the start date of the RRM response.}

\item{rrm_end_date}{The column representing the end date of the RRM response.}

\item{postrrm_start_date}{The column representing the start date of the Post-RRM response.}

\item{postrrm_end_date}{The column representing the end date of the Post-RRM response.}
}
\value{
A merged dataframe containing alert-response data and computed time differences.
}
\description{
This function merges alert, evaluation, and response datasets (RRM & Post-RRM) into a single dataset.
It also calculates key time differences between incident, validation, RRM response, and Post-RRM response.
}
\examples{
# Sample alert data
df_alert <- data.frame(
  uuid = c("A1", "B2", "C3"),
  incident_date = as.Date(c("2024-01-01", "2024-02-01", "2024-03-01")),
  validation_date = as.Date(c("2024-01-05", "2024-02-05", "2024-03-05"))
)

# Sample evaluation data
df_eval <- data.frame(
 uuid = c("A1", "B2", "C3"),
   food_need = c(1, 0, 1)
   )

# Sample RRM response data
df_rrm <- data.frame(
  uuid = c("A1", "B2", "C3"),
  rrm_response_number = c(2, 0, 1),  # B2 has no RRM response
  rrm_start_date = as.Date(c("2024-01-10", NA, "2024-03-10")),
  rrm_end_date = as.Date(c("2024-01-20", NA, "2024-03-15"))
)

# Sample Post-RRM response data
df_postrrm <- data.frame(
  uuid = c("A1", "B2", "C3"),
  postrrm_response_number = c(1, 1, 0),  # C3 has no Post-RRM response
  postrrm_start_date = as.Date(c("2024-01-25", "2024-02-15", NA)),
  postrrm_end_date = as.Date(c("2024-02-01", "2024-02-20", NA))
)

# Run the function
df_combined <- join_alerts_responses_data(
  uuid = "uuid",
  alert_data = df_alert,
  eval_data = df_eval,
  rrm_data = df_rrm,
  rrm_response_number = "rrm_response_number",
  postrrm_data = df_postrrm,
  postrrm_response_number = "postrrm_response_number",
  incident_date = "incident_date",
  validation_date = "validation_date",
  rrm_start_date = "rrm_start_date",
  rrm_end_date = "rrm_end_date",
  postrrm_start_date = "postrrm_start_date",
  postrrm_end_date = "postrrm_end_date"
)
print(df_combined)
}
